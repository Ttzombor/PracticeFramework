<?php
$notifications = App\Notification\NotificationCollector::getNotifications();

if ($notifications) : ?>
    <?php foreach ($notifications as $notification) : ?>
            <div class="alert alert-<?= $notification['type'] ?>" role="alert">
                <?= $notification['message'] ?>
            </div>
    <?php endforeach; ?>
<?php endif; ?>